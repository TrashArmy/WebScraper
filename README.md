# WebScraper for Data Collection
The python script should parse the JSON file. It should create folders and subfolders in the structure:
e.g. -
Aluminium > Cans > <Files>
For the jpeg file names it should just follow the name structure:
<Subfolder Name>+<Counter with 4 digits> e.g. Cans_0001, Cans_1111
